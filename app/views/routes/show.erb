<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />

    <title>Bakery App</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/stylesheets/app_2.css" />
  </head>

   <body>
      <div class="wrapper"> 
         <div class="stops">
            <h1>Welcome to Lakewood Bakery</h1>
            
            <div>
               <br>
               <%if @stops.size != 0 && logged_in? %>

               <h4><a href="/<%=current_user.user_name%>/routes">home</a><h4>
               <h4><a href="/logout">Logout</a><h4>
                
                   <ol>
                   <h2><%=current_user.user_name %> Route <%= @stops[0].route.route_number %> stops</h2><hr><br>
                    <% @stops.each do |stop| %>
                      <li>
                        <a href="/">
                          <%= stop.name %> 
                        </a>
                      </li><br>
                    <% end %>
                    </ol>

               <form method="get" action="/stops/new" style="margin-right: 80%; margin-bottom: 10%">
                  <button type="submit">Add a new stop</button>
               </form>

                <%else%>
                   <p><strong>You don't have stops yet in this route <%=@route.route_number %></strong></p>
                   <form method="get" action="/stops/new" style="margin-right: 80%; margin-bottom: 10%">
                        <button type="submit">Add a new stop</button>
                   </form>
                <%end%>
            </div>
            
            <footer class="branding">
               <small><strong>Big enough to serve, small enough to care</strong></small><br>
               <small>&copy; 2021 <strong>Bakery App</strong></small>
            </footer>
         </div>
      </div>  
   </body> 
<html>