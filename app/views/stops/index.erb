<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />

    <title>Bakery App</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/stylesheets/app_1.css" />
  </head>

   <body>
      <div class="wrapper">
         <div>
            <h1>Welcome to Lakewood Bakery</h1>
            <div>
      
               <form class="logout" method="get" action="/logout" style="margin-right: 80%; margin-bottom: 3%">
                  <button type="submit">Log out</button>
               </form>

               <form class="logout" method="get" action="/account/<%=current_user.user_name%>" style="margin-right: 80%; margin-bottom: 10%;">
                     <button type="submit">Home </button>
               </form>

               <%if current_user.stops.size == 0%>
                  <h3>you don't have any stop yet <%=current_user.user_name %></h3>
               <%else%>
                    <h3><%=current_user.user_name %> stops</h3><hr>
      
                        <ol>
                           <% current_user.stops.each do |stop| %>
                                   <a href="/stops/<%=stop.id%>">
                                      <li style="width: 30%">
                                         <%= stop.name %>
                                      </li><br>
                                   </a>
                           <% end %>
                        </ol>
               <%end%><br>



               <form method="get" action="/stops/new" style="margin-right: 80%; margin-bottom: 10%">
                        <button type="submit">Add a new stop</button>
               </form>
               
              
            </div>
            
            <footer class="branding">
               <small><strong>Big enough to serve, small enough to care</strong></small><br>
               <small>&copy; 2021 <strong>Bakery App</strong></small>
            </footer>
         </div>
      </div>  
   </body> 
<html>